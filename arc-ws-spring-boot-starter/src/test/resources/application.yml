# SPDX-FileCopyrightText: 2024 Deutsche Telekom AG
#
# SPDX-License-Identifier: Apache-2.0

spring:
  jackson:
    default-property-inclusion: NON_NULL
  main:
    banner-mode: off
    web-application-type: reactive
  reactor:
    context-propagation: auto
  graphql:
    graphiql:
      enabled: true

logging:
  level:
    root: INFO
    ai.ancf.lmos.ai: DEBUG

server:
  port: 8080

arc:
  subscriptions:
    events:
      enable: true